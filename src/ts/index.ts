// Sample TypeScript file to test ESLint
import { typed_add, typed_greet } from 'ctype/core';

// Test the typed add function
const sum = typed_add(5, 3);
console.log(`Sum: ${sum}`);

// Test the typed greet function
const greeting = typed_greet('TypeScript');
console.log(greeting); 