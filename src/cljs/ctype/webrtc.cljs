(ns ctype.webrtc
  (:require [cljs.nodejs :as nodejs]))

(defn start-camera []
  (println "Starting camera..."))

(defn stop-camera []
  (println "Stopping camera..."))

(defn switch-camera []
  (println "Switching camera...")) 